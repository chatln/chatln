<% if status_user(current_user) %>
<script>
  $('#summernote').summernote({
    placeholder: 'Le contenu du Renforcément ici ...',
      tabsize: 2,
      height: 200,
      minHeight: null,             // set minimum height of editor
      maxHeight: null,             // set maximum height of editor
      focus: true,                 // set focus to editable area after initializing summernote
      toolbar: [
          ['font', ['bold', 'underline', 'clear']],
          ['color', ['color']],
          ['inser', ['link', 'picture']],
          ['view', ['fullscreen']]

        ],
      codeviewFilter: false,
      codeviewIframeFilter: true
      });

</script>
<% else %>
<script>
  $('#summernote').summernote({
    placeholder: 'Prends les photos du sujet et la correction, puis cliquez sur icone image pour les  ajouter, faite un petit commentaire au desous...',
      tabsize: 2,
      height: 200,
      minHeight: null,             // set minimum height of editor
      maxHeight: null,             // set maximum height of editor
      focus: true,                 // set focus to editable area after initializing summernote
      toolbar: [
          ['font', ['bold', 'underline', 'clear']],
          ['color', ['color']],
          ['inser', ['picture']],
          ['view', ['fullscreen']]

        ],
      codeviewFilter: false,
      codeviewIframeFilter: true
      });

</script>
<% end %>
